# mgmt_operations

Project for tracking changes to the archiver via the mgmt operations interface. For example, change types, complicated renames, adding aliases.

## Installation

```console
pip install archiver-mgmt-operations
```

## Usage
